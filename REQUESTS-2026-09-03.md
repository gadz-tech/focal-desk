# Requests — 2026-09-03 (dictated by Ryan, captured by Cowork)

Dated record, one item. Continues `REQUESTS-2026-08-28.md`; who acts next is
named there (lane 1). Committed by Worker F (`box/WORKER-PROMPTS-2026-09-03.md`).

## 1 · DATA POINT for §1 (Fusion floats) — KiCad floats too, until restarted

**Ryan's words (2026-09-03):** "KiCad also had a floating window problem but
once I rebooted KiCad it was fine. It also has multiple windows it seems and
that might be what causes the problem."

- Second app with the symptom; second app that opens **more than one
  top-level window** (KiCad: project manager → schematic / pcbnew / 3D
  viewer, each its own HWND; Fusion: splash → main). Strengthens the
  08-28 mechanism (a) — a later HWND is not being (re)evaluated — over
  (b), and adds a third candidate: **owner/parent chains**: `is_manageable`
  may be rejecting owned windows, or the first-seen HWND is the owner and
  the real window is its child.
- "Fine after a restart of KiCad" = the manageable decision is right when
  the window exists *at scan time* and wrong when it *arrives later*.
  Whatever the fix, the test to pin is the 08-28 one: "a window that
  becomes manageable later gets managed."
- Still lane 1's work (log capture during a launch, then the fix). Not
  today: Ryan lives in Fusion and KiCad today, and focal-desk must not run
  while he does.
