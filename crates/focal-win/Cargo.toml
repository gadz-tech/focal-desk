[package]
name = "focal-win"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
# The Win32 layer is reference code until first built on a real Windows
# machine. Flip this on there, and add under
# [target.'cfg(windows)'.dependencies]:
#   windows = { version = "0.58", features = [
#     "Win32_Foundation", "Win32_Graphics_Dwm", "Win32_Graphics_Gdi",
#     "Win32_UI_Accessibility", "Win32_UI_WindowsAndMessaging",
#     "Win32_UI_HiDpi", "Win32_System_Threading",
#   ] }
win32 = []

[dependencies]
focal-core = { path = "../focal-core" }
